# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
dist-ssr
*.local

# Environment — NUNCA commitar segredos
.env
.env.*
!.env.example

# WhatsApp server — sessões Baileys e deps (NUNCA commitar credenciais de sessão)
auth_session
whatsapp-server/node_modules
whatsapp-server/auth_session
# whatsapp-server é um repo Git separado (deploy próprio no Railway) — não embutir aqui
/whatsapp-server

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs
*.log

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# Vercel
.vercel

# Capacitor — web assets copiados para dentro das plataformas (gerados por `cap sync`)
android/app/src/main/assets/public
ios/App/App/public
capacitor.config.json

# Android nativo
android/.gradle
android/build
android/app/build
android/local.properties
android/.idea
android/captures
android/*.iml
android/keystore.properties
*.keystore
*.jks

# iOS nativo
ios/App/Pods
ios/App/build
ios/App/output
ios/App/App/public
ios/DerivedData
*.mobileprovision
*.p12
